# Runtime dependencies. In production these come from the NVIDIA PyTorch base
# image (torch, tensorrt) plus the pip installs in the Dockerfile.
# Pinned: mediapipe >=0.10.19 dropped the legacy `mp.solutions.hands` API the
# code uses (only the Tasks API remains). 0.10.18 also constrains numpy<2.
mediapipe==0.10.18
opencv-python-headless
# Pin to the v5 API the UI targets (Gradio 6 moved theme=/css= to launch()).
gradio>=5,<6
tritonclient[http,grpc]
pyyaml
pandas
numpy
# ONNX export (torch.onnx) — onnxscript is required by recent torch exporters.
onnx
onnxscript
